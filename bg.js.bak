
chrome.extension.onMessage.addListener(function(request, sender, sendResponse) {

	if(request.cmd=='displayUpdate'){
alert(request.updateNum);
		chrome.browserAction.setBadgeText({text:request.updateNum});
		
	}
});
  